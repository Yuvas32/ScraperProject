const Navbar = () => {
  return (
    <nav
      style={{
        backgroundColor: "#2c3e50",
        color: "white",
        padding: "1rem 2rem",
        textAlign: "center",
        fontWeight: "bold",
        fontSize: "1.2rem",
      }}
    >
      My React Dashboard
    </nav>
  );
};

export default Navbar;
